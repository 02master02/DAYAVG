# 使用说明

## 当前阶段

当前仓库只完成了工作流初始化，还没有具体业务程序。

## 安装依赖

```bash
# 暂无依赖
```

## 运行程序

```bash
# 暂无可运行入口
```

## 输入文件放哪里

当前未定义真实输入格式。后续请在明确需求后补充到 `docs/task_goal.md`，并根据实现决定是否放在项目根目录、专用数据目录或外部路径。

## 输出结果在哪里

默认放在 `outputs/`。

## 测试方式

```bash
# 当前只有结构级检查
git status --short
Get-ChildItem -Recurse
```

## 常见问题

### 为什么现在没有业务代码？

因为用户本轮只启用了工作流技能，没有提供具体功能需求。按照该工作流，先完成文档和结构初始化，再进入实现阶段。

### 为什么没有运行技能自带的 Python 初始化脚本？

当前环境直接调用 `python.exe` 失败，因此本轮使用手动方式完成初始化。
